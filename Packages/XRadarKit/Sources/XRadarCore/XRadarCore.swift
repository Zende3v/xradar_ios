/// Pure domain layer of x_radar: models, geometry, route maths, report
/// relevance, guidance text and sun cycle. Foundation only — no UIKit, SwiftUI
/// or CoreLocation — so every rule stays unit-testable.
public enum XRadarCore {}
